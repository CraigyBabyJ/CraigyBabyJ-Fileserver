# CraigyBabyJ-Fileserver - Environment Configuration
# Copy this file to .env and update the values below

# Site Configuration
# DEPRECATED: SITE_PASSWORD is no longer used for authentication
# Use the user configuration below instead
SITE_PASSWORD=changeme123

# User Authentication Configuration
# Define users that can access the system using environment variables
# Format 1 (Simple): USER_USERNAME=password
USER_CRAIG=mypassword123
USER_ADMIN=adminpassword456

# Format 2 (Detailed): USER_USERNAME_USERNAME=actual_username & USER_USERNAME_PASSWORD=password
# USER_JOHN_USERNAME=john
# USER_JOHN_PASSWORD=johnpassword789

# Admin Privileges
# Set to 'true' to give admin access (can manage users, view analytics)
USER_CRAIG_ADMIN=true
USER_ADMIN_ADMIN=true
# USER_JOHN_ADMIN=false

# Flask Secret Key - Keep this secret and don't change it once set
# This keeps user sessions valid between server restarts
SECRET_KEY=your-secret-key-here-change-this-to-something-random

# Production Settings
# Set to 'true' when deploying to disable debug mode and enable security features
PRODUCTION_MODE=false

# Flask Application Settings
# Host and port for the Flask app (0.0.0.0 allows external connections)
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Security Settings
# Maximum file upload size in MB
MAX_UPLOAD_SIZE=500

# Rate limiting settings (requests per minute for login attempts)
LOGIN_RATE_LIMIT=5

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Change SITE_PASSWORD to a secure password
# 3. Generate a random SECRET_KEY (you can use: python -c "import secrets; print(secrets.token_hex(32))")
# 4. Set PRODUCTION_MODE=true when deploying to production
# 5. Adjust other settings as needed for your deployment